server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://172.17.17.137:3306/mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    elasticsearch:
      repositories:
        enabled: true
  elasticsearch:
    rest:
      uris: http://172.17.17.161:9200
      # 多个节点用逗号分隔
      # uris: http://172.17.17.161:9200,http://172.17.17.162:9200,http://172.17.17.163:9200
      # 用户名，默认空
      # username: 
      # 密码，默认空
      # password:
      # 连接超时时间（秒）
      connection-timeout: 1
      # 通信超时时间（秒）
      ssocket-timeout: 30
      # 路径前缀，默认空
      path-prefix: 

mybatis:
  mapper-locations:
    - classpath*:net/**/mapper/*.xml