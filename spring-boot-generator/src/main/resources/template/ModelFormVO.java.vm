package ${package}.${moduleName}.model.vo.form;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import javax.validation.constraints.NotEmpty;
import javax.validation.constraints.NotNull;
import java.io.Serializable;
#if(${hasBigDecimal})
import java.math.BigDecimal;
#end
#if(${hasDate})
import com.fasterxml.jackson.annotation.JsonFormat;
import java.util.Date;
#end

/**
 * ${comments}提交对象
 *
 * @author ${author}
 * @date ${datetime}
 */
@Data
@Schema(description = "${comments}提交对象")
public class ${className}FormVO implements Serializable {

    private static final long serialVersionUID = 1L;

#foreach($column in $columns)
#if($column.columnName != $pk.columnName && 
    $column.columnName.toLowerCase() != "create_user_id" && 
    $column.columnName.toLowerCase() != "create_time" && 
    $column.columnName.toLowerCase() != "update_user_id" && 
    $column.columnName.toLowerCase() != "update_time" && 
    $column.columnName.toLowerCase() != "del_flag")
    /**
     * ${column.comments}
     */
    #if(!$column.isNullable)
    #if(${column.attrType} == "String")
    @NotEmpty(message = "${column.comments}不能为空")
    #else
    @NotNull(message = "${column.comments}不能为空")
    #end
    #end
    #if(${column.attrType} == "Date")
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "Asia/Shanghai")
    #end
    @Schema(description = "${column.comments}")
    private ${column.attrType} ${column.attrname};
    
#end
#end
}